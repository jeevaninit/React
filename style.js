// styles.js

export const style = {
    color: {
      base: {
        black: {
          value: '#000000',
        },
        gray: {
          11: {
            value: '#f2f2f2',
          },
          // add more shades of gray if needed
        },
        // add more colors if needed
      },
      // add more color categories if needed
    },
    // add more style categories if needed
  };
  

  /* 
  
  import { css } from '@emotion/css';
import { style } from './styles';

const myColorStyle = css({
  color: style.color.base.black.value,
});

function MyComponent() {
  return (
    <div className={myColorStyle}>
      <h1>Hello, World!</h1>
      <p>This is an example of using the myColorStyle style.</p>
    </div>
  );
}

export default MyComponent;

  
  */